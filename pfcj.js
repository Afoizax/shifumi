function choix(user) {
        choixpc = Math.floor(Math.random() * Math.floor(3))
        console.log(choixpc)

        var choixuser = document.getElementById(user).value
        console.log(choixuser)
    }


function choixpc() {
    
}